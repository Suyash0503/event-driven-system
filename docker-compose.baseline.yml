version: "3.9"

services:
  rabbitmq:
    image: rabbitmq:3.11-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest

  api_gateway:
    build: ./api_gateway
    container_name: api_gateway
    ports:
      - "8000:8000"
    depends_on:
      - user_service_v1
      - user_service_v2
      - order_service
    environment:
      - GATEWAY_CONFIG=/app/gateway_config.json
      - AGENT_MODE=false

  user_service_v1:
    build: ./user_service_v1
    container_name: user_service_v1
    ports:
      - "8002:8002"
    environment:
      - MONGO_URL=${MONGO_URL}
      - RABBIT_URL=amqp://guest:guest@rabbitmq:5672/
    depends_on:
      - rabbitmq

  user_service_v2:
    build: ./user_service_v2
    container_name: user_service_v2
    ports:
      - "8003:8003"
    environment:
      - MONGO_URL=${MONGO_URL}
      - RABBIT_URL=amqp://guest:guest@rabbitmq:5672/
    depends_on:
      - rabbitmq

  order_service:
    build: ./order_service
    container_name: order_service
    ports:
      - "8001:8001"
    environment:
      - MONGO_URL=${MONGO_URL}
      - RABBIT_URL=amqp://guest:guest@rabbitmq:5672/
    depends_on:
      - rabbitmq
